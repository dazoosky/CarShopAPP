{% extends 'base.html.twig' %}

{% block body %}
    <h1>Workordercategory</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ workOrderCategory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ workOrderCategory.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('workordercategories_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('workordercategories_edit', { 'id': workOrderCategory.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
